FROM alpine:3.19

RUN apk add --no-cache bash shadow busybox-extras

WORKDIR /app

# Placeholder: copy built meshcore-cli binary here if you have it
# COPY ./bin/meshcore-cli /usr/local/bin/meshcore-cli

CMD ["/bin/sh", "-lc", "echo 'meshcore-cli placeholder container'; tail -f /dev/null"]

